---
- name: "pip install docker-compose"
  pip:
    name: docker-compose
    extra_args: --user

- name: "pip install virtualenv"
  pip:
    name: virtualenv
    extra_args: --user

- name: "pip install pre-commit"
  pip:
    name: pre-commit
    extra_args: --user
